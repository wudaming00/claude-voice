# Claude Voice — Python dependencies
# Install in a fresh venv:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

fastapi>=0.115
uvicorn>=0.30
faster-whisper>=1.1
edge-tts>=7.0
av>=13.0
